using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class TimerUI : MonoBehaviour
{
    /*[SerializeField] GameStateManager _gameTimer;
    [SerializeField]Image _img;
    float currentRiftTime => _gameTimer.CurrentRiftTime;
    // Update is called once per frame
    void Update()
    {
        if(currentRiftTime <= 0) return;
        float fillAmount = currentRiftTime / World.RiftDurationInSeconds;
        _img.fillAmount = fillAmount;
    }*/
}
